# Copy this file to .env

# Worker URL for bulk import
# - Set URL to use worker (testing, no API key needed)
# - Leave empty for direct XAI API (production)
# WORKER_URL=https://quote-parser.gabkolistiak.workers.dev
WORKER_URL=

# Sentry crash reporting (optional)
# Get these values from sentry.io after creating a project
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
# Auth token for source map uploads (set in CI or local env)
# SENTRY_AUTH_TOKEN=
